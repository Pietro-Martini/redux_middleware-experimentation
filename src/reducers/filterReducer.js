const initialState = () => ({
  minYear: ''
})

export const types = {}

export const actions = {}

export default function filterReducer (state = initialState(), {payload, type}) {
  switch (type) {
    default:
      return state
  }
}
